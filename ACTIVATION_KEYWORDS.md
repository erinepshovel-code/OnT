# OnT Activation Keywords

Status: provisional lexical registry

Purpose: provide a conservative trigger surface for raising **OnT — Thoughts on the Threshold of Project Status** during a live thread until activation can be correlated against historical EDCM traces and projects pursued to completion.

`raise OnT` means: mark a newly forming project candidate for compact capture. It does not by itself create a repository, promote a claim, alter canon, authorize implementation, or interrupt work already underway.

## Seed activation table

| Signal class | Activating words or phrases | Strength | Use condition |
|---|---|---:|---|
| Explicit OnT | `OnT`; `on the threshold`; `thought on the threshold`; `thoughts on the threshold`; `threshold of project status`; `raise OnT`; `put this in OnT` | hard | Raise unless the phrase is quoted, historical, or explicitly negated. |
| Preserve for later | `needs a note`; `note somewhere`; `future reference`; `pick up later`; `park this`; `save this`; `return to this`; `come back to this`; `not now`; `defer` | strong | Must refer to a concrete idea, domain, mechanism, artifact, or unanswered project question. |
| Project formation | `this should be a thing`; `make this a skill`; `needs its own repo`; `create a repo`; `build this`; `application`; `module`; `tool`; `workflow`; `vertical slice`; `project status` | strong | Must identify or imply a purpose, artifact, or operating boundary—not merely mention the word. |
| Structural placement | `same treatment`; `just like`; `belongs in`; `use the existing pattern`; `not a standalone note`; `for METAPAT`; `for UCNS`; `for EDCM`; `for skill-lib`; `downstream of` | strong | Requires a named existing project, pattern, application family, or architectural relation. |
| Structured proposal packet | `ratifiable proposal`; `Skill:`; `Purpose:`; `Pipeline:`; `Calibration:`; `Immediate targets:`; `Gate:`; `Status tags`; `manifest-first`; `no code until ratified` | hard pattern | Raise an **unratified** candidate when at least four distinct labeled fields co-occur in one submitted proposal. Structure signals project threshold; it does not imply approval. |
| Commitment | `do it to it`; `do this`; `proceed`; `commence`; `deposit`; `approve`; `approved path`; `implement`; `merge`; `push`; `finish`; `continue` | amplifier | Never activates alone. It strengthens an already forming candidate and records willingness to pursue it. |
| Evidence and completion | `what is needful`; `next empirical step`; `ready for real-system`; `first PR`; `release`; `tests`; `evidence`; `evidence locker`; `handoff`; `ship` | amplifier | Requires a project-formation, placement, preservation, or structured-proposal signal in the same thread. |
| Boundary and importance | `must`; `load-bearing`; `highest priority`; `immediate task`; `scope boundary`; `preserve`; `exact`; `do not change`; `fail-closed`; `no transfer`; `known defect`; `silent inheritance` | amplifier | Signals seriousness and constraint density, not project status by itself. |
| Recurrent named concept | a distinctive noun or phrase repeated across turns and made more specific after correction | strong pattern | Add the concept to the live candidate vocabulary for that thread. Recurrence without increasing specificity is not enough. |
| Provenance declaration | `Claude generated`; `GPT generated`; `context, prompt by`; `human edited`; `source:`; `derived from` | qualifier | Preserve exact origin. Provenance never activates alone and never converts submitted material into user ratification. |

## Provisional activation rule

Raise OnT when either:

1. one unnegated **hard** signal appears; or
2. one **hard pattern** is satisfied; or
3. two **strong** signals from different classes appear in the same thread; or
4. one **strong** signal appears with two distinct **amplifiers**.

A match should be delayed until a natural response boundary when immediate execution is already in progress.

## Suppression table

| Suppressor | Examples | Effect |
|---|---|---|
| Explicit rejection | `do not save`; `not a project`; `do not pursue`; `discard this`; `never mind` | Do not raise OnT. |
| Idle inquiry | `just curious`; `hypothetically`; a single explanatory question with no placement, preservation, formation, or structured-proposal signal | Do not raise OnT. |
| Bare project vocabulary | isolated use of `project`, `repo`, `skill`, `build`, or `application` | Insufficient without context. |
| Existing active work | instructions that only continue an already recognized project | Do not create a duplicate OnT candidate; only raise a newly branching thought. |
| Quotation or analysis | trigger language quoted from another source or discussed only as an example | Do not treat quoted language as user commitment. A complete, intentionally submitted, provenance-labeled proposal may still be raised as an **unratified candidate**. |

## Seed trace: corvid communication

The following sequence is a clear positive example:

- `this should be a thing for metapat/ucns/EDCM`
- `requires a note somewhere`
- `same treatment`
- `future reference to pick up later`
- `OnT`
- `do it to it`

Result: raise an OnT candidate for a future corvid-communication application spanning METAPAT, UCNS, and EDCM.

## Seed trace: EDCM semantic drift

The submitted proposal contained:

- `Ratifiable proposal`
- `Skill: drift`
- explicit `Purpose`, `Pipeline`, `Calibration`, `Immediate targets`, `Gate`, and `Status tags` fields
- `manifest-first — no code until this is ratified`
- a named calibration specimen and cosmetic control
- an upstream defect gate with prohibited silent inheritance
- explicit provenance: `Claude generated. Context, prompt by Erin Patrick Spencer.`

Result: raise an **unratified** OnT candidate for an EDCM-scored semantic-diff skill. Preserve the proposal, provenance, and ratification blockers without creating skill code or inferring approval.

## Provisional EDCM linkage

The keyword table is a temporary proxy, not an EDCM reading. Future work should compare candidate activations with historical completed-project threads for patterns including:

- increasing constraint resolution and specificity;
- correction incorporation that changes structure rather than wording;
- persistent recurrence without non-integrative looping;
- movement from exploratory language to bounded placement and action;
- movement from a name and purpose into pipeline, calibration, gate, and evidence status;
- explicit treatment of upstream defects rather than silent inheritance;
- low overhead relative to concrete project formation.

Only correlations that distinguish completed projects from abandoned or merely discussed ideas should survive into an executable OnT skill.

## hmmm

No empirical activation thresholds, completed-project comparison corpus, false-positive study, or executable `SKILL.md` exists yet. The registry now distinguishes project-threshold structure from user ratification: generated material can supply a candidate worth preserving without acquiring authority merely because it is polished.